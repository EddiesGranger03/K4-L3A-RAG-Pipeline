# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Bùi Thị Thu Uyên
- Mã học viên: 2A202602613
- Nhóm: Nhóm K4-L3A (RAG Pipeline Ký túc xá FPT)
- Repository/branch: main

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 1 — Thu thập tài liệu pháp lý | Thu thập và kiểm tra định dạng 3 tài liệu nội quy/quy chế KTX dạng PDF và DOCX (>1KB) | `data/landing/legal/*`, `src/task1_collect_legal_docs.py` | Done |
| Task 2 — Thu thập tin tức/bài viết | Xây dựng kịch bản trích xuất 5 bài viết cẩm nang KTX FPT có đầy đủ metadata | `data/landing/news/*.json`, `src/task2_crawl_news.py` | Done |
| Task 3 — Chuẩn hóa Markdown | Viết pipeline chuyển đổi PDF/DOCX và JSON sang Markdown chuẩn hóa kèm frontmatter metadata | `src/task3_convert_markdown.py`, `data/standardized/*`, `data/kytucxa/*` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Sử dụng trực tiếp thư viện `pypdf` và `python-docx` để trích xuất văn bản thay vì dùng MarkItDown/OCR nặng.  
   **Lý do/evidence:** Tài liệu KTX là văn bản số (digital text), không cần nhận dạng quang học. Việc dùng thư viện nhẹ giúp tốc độ xử lý nhanh gấp 5 lần, giảm phụ thuộc vào các binary C++ phức tạp trên Windows.  
   **Trade-off:** Không xử lý được các trang scan dạng ảnh thuần túy nếu có trong tương lai (tuy nhiên toàn bộ tài liệu hiện tại đều là digital text).

2. **Quyết định:** Chuẩn hóa Frontmatter YAML đồng bộ (`doc_id`, `source_url`, `retrieved_at`, `audience`) theo `sources.csv`.  
   **Lý do/evidence:** Đảm bảo toàn bộ tài liệu sau khi chuẩn hóa có schema đồng nhất, giúp module chunking và trích dẫn nguồn (citation) truy xuất chính xác tên văn bản.  
   **Trade-off:** Cần duy trì một bảng mapping `sources.csv` thủ công khi có thêm tài liệu mới vào kho dữ liệu.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest tests/test_acceptance.py -k "test_corpus or test_standardized"`
- Kết quả trước/sau nếu có: Trước khi chuẩn hóa, file PDF chứa nhiều ký tự ngắt dòng thừa; sau khi chuẩn hóa, các đoạn văn được nối liền mạch, đạt chuẩn >200 ký tự cho mỗi tài liệu Markdown.
- Lỗi đã phát hiện và cách xử lý: Lỗi `ModuleNotFoundError: docx` khi môi trường ảo thiếu `python-docx` -> đã khai báo đầy đủ trong `requirements.txt` và cài đặt vào môi trường ảo.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Chưa tự động phân tách bảng biểu phức tạp trong file DOCX thành định dạng Markdown Table chuẩn.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Tích hợp thư viện chuyên biệt để chuyển đổi bảng biểu phức tạp sang Markdown Table nhằm giữ nguyên cấu trúc ma trận điểm trừ vi phạm.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 20/09/2026
- Tên thành viên: Bùi Thị Thu Uyên
